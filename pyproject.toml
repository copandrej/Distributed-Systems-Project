[project]
name = "distributed-systems-project"
version = "0.1.0"
description = "AI-powered agent system for managing Kubernetes clusters"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "crewai>=0.80.0",
    "crewai-tools>=0.12.0",
    "python-dotenv>=1.0.0",
    "litellm>=1.0.0",
    "aiohttp>=3.13.2",
]
